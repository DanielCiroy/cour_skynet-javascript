/*
let nombre1 = parseInt(prompt('donner le premier nombre'));
let nombre2 = parseInt(prompt('donner le deuxieume nombre'));
function addition(a, b) {
    if (parseInt(a) && parseInt(b)) {
        return alert(a + b);
    }
    else {
        alert('veuillez saisir des nombres entier')
    }

}


addition(nombre1, nombre2)
*/

tab = ['dani', 'john', 'ciroy'];

for (let i = 0; i <= tab.length; i++) {
    console.log(tab[i]);
}

